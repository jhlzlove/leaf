{
  "type": "module",
  "scripts": {
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs",
    "dev": "set NODE_OPTIONS=--openssl-legacy-provider && vitepress dev docs",
    "build": "set NODE_OPTIONS=--openssl-legacy-provider && vitepress build docs"
  },
  "config": {
    "commitizen": {
      "path": "czg",
      "czConfig": "./cz.config.cjs"
    }
  },
  "devDependencies": {
    "vitepress": "^1.3.4"
  }
}
