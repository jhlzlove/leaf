name: deploy project docs

on:
  push:
    # 分支
    branches: [ dev ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@pages

      - name: deploy
        run: ./deploy.sh
        shell: bash
